<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deriza.appstutorial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewItemTutorial" />
        <activity android:name=".warnalink" />
        <activity android:name=".frame" />
        <activity android:name=".About" />
        <activity android:name=".link" />
        <activity android:name=".header" />
        <activity android:name=".form" />
        <activity android:name=".select"></activity>
    </application>

</manifest>